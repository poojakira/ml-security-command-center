# ml-security-command-center

Point-in-time inventory script. Counts test declarations and rule identifiers across sibling repos, records Git revisions, renders a static HTML report. Zero dependencies. Not a monitoring service.

```bash
python generate_metrics.py  # writes metrics.json, updates index.html
```
